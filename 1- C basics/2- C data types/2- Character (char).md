Used to store single characters like 'a', 'b' the %c format specifier is used to print it.

Alternatively ASCII can be used to display certain characters.
```
char a = 65, b = 66, c = 67;  
printf("%c", a);  
printf("%c", b);  
printf("%c", c);
```

Every char has its proper ASCII number.